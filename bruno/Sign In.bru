meta {
  name: Sign In
  type: http
  seq: 2
}

post {
  url: {{url}}/auth/local/signin
  body: json
  auth: none
}

body:json {
  {
    "email":"john@example.com",
    "password":"123456"
  }
}

vars:post-response {
  access_token: res.body.access_token
  refresh_token: res.body.refresh_token
}
