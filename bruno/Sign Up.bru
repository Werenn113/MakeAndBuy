meta {
  name: Sign Up
  type: http
  seq: 2
}

post {
  url: {{url}}/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email":"toto@example.com",
    "password":"123456"
  }
}

vars:post-response {
  access_token: res.body.access_token
  refresh_token: res.body.refresh_token
}
